/**
 * `RadixTree` Reference Implementation.
 *
 * In computer science, a radix tree (also radix trie or compact prefix tree) is
 * a data structure that represents a space-optimized trie in which each node
 * that is the only child is merged with its parent.
 *
 * https://en.wikipedia.org/wiki/Radix_tree
 *
 * @author Josh Black
 * @flow
 */

// type EdgeType = {
  // targetNode: NodeType;
  // label: string;
// };

// type NodeType = {
  // edges: Array<EdgeType>;
  // isLeaf: () => boolean;
// };

// export default class RadixTree {
  // _root: NodeType;

  // constructor() {
    // this._root = {
      // edges: [],
    // };
  // }

  /**
   * Determines if a string exists in a trie.
   */
  // lookup(string: String): boolean {
  // }
// }
